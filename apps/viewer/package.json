{
  "name": "@integration-telegram-app/viewer",
  "private": true,
  "version": "0.2.1",
  "scripts": {
    "start": "vite --open",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check-types": "tsc --noEmit"
  }
}
